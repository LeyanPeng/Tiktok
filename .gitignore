# Organiser-supplied material. Not redistributed here — see README for how to
# obtain it from the official participant kit.
data/
evaluator/
starter/
docs/competition_specification.md
docs/submission_rules.md
docs/agent_api_contract.json
docs/evaluation_config.json
docs/baseline_results.json

# Working notes and internal process records. Kept locally, not part of the
# submission.
PROGRESS.md
BLOCKED.md
docs/DEVPOST*.md
docs/DEMO_SCRIPT.md
review-archive/

# Video. The demo lives on YouTube (linked from README and TECHNICAL_REPORT);
# the raw capture is 220 MB, past GitHub's 100 MB file limit, and a rendered copy
# in a code repository serves no reviewer who can already watch it.
# Kept locally, not committed.
*.mp4
*.mov
*.mkv
*.srt
*.ass
Demo_vedio/

# Evaluation output
results*.json
runs/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Secrets: never committed
.env
*.key
*.pem
